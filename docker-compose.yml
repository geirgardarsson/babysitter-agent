services:
  babysitter:
    build: .
    restart: unless-stopped
    ports:
      - "${PORT:-3456}:${PORT:-3456}"
    volumes:
      - ./content:/app/content
      - ./data:/app/data
    env_file:
      - .env
    environment:
      - CONTENT_DIR=/app/content
